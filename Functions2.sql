
select EMPLOYEE_ID, FIRST_NAME, calexp(hire_date)
from employees;

# Max salary in given dept

select maxSalary_dept(60);

#number of employees in a departmentname
# total salary in a departmentname 

